{{ snippets['PEPP-BasicForm-Library'] }}

// Initialize with inline config
PEPPBasicForm.init({
  richTextFields: ['cs_richtextfield'],
  features: {
    lookupButtons: true,
    lookupModals: true,
    richTextEditor: true
  }
});